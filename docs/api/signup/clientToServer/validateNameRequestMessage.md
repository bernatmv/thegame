# ValidateNameRequestMessage

```
{
    kind: string;
    name: string;
}
```

    `kind` => *required* => The message type
    `name` => *required* => The name to validate in the signup process (check if already exists, etc)