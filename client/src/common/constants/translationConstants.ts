export const keyPrefix = 'thegame.';

export default class TranslationConstants {
    static get loginInputPlaceholder() {
        return `${keyPrefix}loginInputPlaceholder`;
    }

    static get saySomething() {
        return `${keyPrefix}saySomething`;
    }

    static get online() {
        return `${keyPrefix}online`;
    }

    static get connecting() {
        return `${keyPrefix}connecting`;
    }

    static get offline() {
        return `${keyPrefix}offline`;
    }

    static get aMomentAgo() {
        return `${keyPrefix}aMomentAgo`;
    }

    static get secondsAgo() {
        return `${keyPrefix}secondsAgo`;
    }

    static get minute() {
        return `${keyPrefix}minute`;
    }

    static get hour() {
        return `${keyPrefix}hour`;
    }

    static get ago() {
        return `${keyPrefix}ago`;
    }

    static get server() {
        return `${keyPrefix}server`;
    }

    static get north() {
        return `${keyPrefix}north`;
    }

    static get east() {
        return `${keyPrefix}east`;
    }

    static get south() {
        return `${keyPrefix}south`;
    }

    static get west() {
        return `${keyPrefix}west`;
    }

    static get up() {
        return `${keyPrefix}up`;
    }

    static get down() {
        return `${keyPrefix}down`;
    }

    static get unvisitedLocation() {
        return `${keyPrefix}unvisitedLocation`;
    }

    static get unvisitedDirection() {
        return `${keyPrefix}unvisitedDirection`;
    }

    static get welcomeToTheGame() {
        return `${keyPrefix}welcomeToTheGame`;
    }

    static get noObjectsCloseBy() {
        return `${keyPrefix}noObjectsCloseBy`;
    }

    static get noEnemiesCloseBy() {
        return `${keyPrefix}noEnemiesCloseBy`;
    }

    static get thereIs() {
        return `${keyPrefix}thereIs`;
    }

    static get youSee() {
        return `${keyPrefix}youSee`;
    }

    static get isHere() {
        return `${keyPrefix}isHere`;
    }

    static get introLine1() {
        return `${keyPrefix}introLine1`;
    }

    static get introLine2() {
        return `${keyPrefix}introLine2`;
    }

    static get introLine3() {
        return `${keyPrefix}introLine3`;
    }
}